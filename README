Custom ELF Loader intended for pedagogic use

Chaque étape possède son propre Makefile. Si vous voulez compiler à la main vous
pouvez éxecuter la commande 'make' et 'make clean' pour supprimer les fichiers
compilés.

Pour l'utiliser de manière automatisé, il suffit de lancer le script shell
'tests.sh' et de suivre les instructions.
